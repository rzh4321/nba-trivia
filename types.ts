export type MCQuestionType = {
    question: string;
    choices: string[];
    correct: string;
    index: number;
};

export type TFQuestionType = {
    question: string;
    correct: string;
    index: number;
};

